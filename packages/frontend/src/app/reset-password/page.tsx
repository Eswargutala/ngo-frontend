// Page Component
import ResetPassword from '../../views/ResetPassword';

export const metadata = {
  title: 'Reset Password | LakhilRaj',
  description: 'Reset your password securely.',
};

export default function ResetPasswordPage() {
  return <ResetPassword />;
}
